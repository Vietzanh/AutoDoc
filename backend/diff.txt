diff --git a/backend/src/docx_generator/processors.py b/backend/src/docx_generator/processors.py
index ad1cc16..f870cb3 100644
--- a/backend/src/docx_generator/processors.py
+++ b/backend/src/docx_generator/processors.py
@@ -426,9 +426,26 @@ def process_text_block(
 
     x0_in = x0_pdf / 72
     x1_in = x1_pdf / 72
-    indent_from_margin_in = max(0.0, x0_in - doc_left_margin_in - (5.0 / 72.0))
-    right_edge_in = page_width_in - doc_right_margin_in
-    right_indent_from_margin_in = max(0.0, right_edge_in - x1_in - (5.0 / 72.0))
+
+    is_multicolumn = hasattr(block, 'extra') and block.extra.get("is_multicolumn_pdf", False)
+    in_column = hasattr(block, 'extra') and block.extra.get("in_column", False)
+
+    if in_column:
+        # Inside a Word 2-column section: use relative left indent
+        if "col_left_x0_pt" in block.extra:
+            col_left_x0_in = block.extra["col_left_x0_pt"] / 72.0
+            indent_from_margin_in = max(0.0, x0_in - col_left_x0_in)
+        else:
+            indent_from_margin_in = 0.0
+    else:
+        indent_from_margin_in = max(0.0, x0_in - doc_left_margin_in - (5.0 / 72.0))
+
+    if is_multicolumn:
+        # For multi-col documents, completely disable strict right indent to prevent 1-letter lines
+        right_indent_from_margin_in = 0.0
+    else:
+        right_edge_in = page_width_in - doc_right_margin_in
+        right_indent_from_margin_in = max(0.0, right_edge_in - x1_in - (5.0 / 72.0))
 
     should_merge = (last_paragraph is not None)
 
@@ -492,26 +509,54 @@ def process_text_block(
 
         for group_idx, block_idx in enumerate(sorted_block_indices):
             group_with_order = sorted(text_block_groups[block_idx], key=lambda x: x[1])
-            group = [elem for elem, _ in group_with_order]
-
-            if group_idx > 0:
-                p = docx_doc.add_paragraph(style=style_name)
-                p.paragraph_format.left_indent = Inches(indent_from_margin_in)
-                p.paragraph_format.right_indent = Inches(right_indent_from_margin_in)
-                p.paragraph_format.space_after = Pt(2)
-                if block_type == "title":
-                    p.alignment = WD_ALIGN_PARAGRAPH.CENTER
-                elif block_type in ["figure_caption", "table_caption"]:
-                    p.alignment = WD_ALIGN_PARAGRAPH.LEFT
-                elif block_type not in ["formula_caption"] and not style_name.startswith("Heading"):
-                    p.alignment = WD_ALIGN_PARAGRAPH.JUSTIFY
-
-            for elem in group:
-                text_content = elem.text
-                if not text_content.endswith(" "):
-                    text_content += " "
-                run = p.add_run(sanitize_text_for_xml(text_content))
-                _apply_span_style(run, elem)
+            group_elements = [elem for elem, _ in group_with_order]
+
+            # Subdivide YOLO blocks into multiple paragraphs if there are gaps or hanging indents
+            subgroups = []
+            if is_multicolumn and group_elements:
+                font_sizes = [e.font_size for e in group_elements if e.font_size is not None]
+                avg_font_size = sum(font_sizes) / len(font_sizes) if font_sizes else 12.0
+                vg_thresh = avg_font_size * 0.4  # Sensitive vertical gap for references
+                
+                curr_sg = [group_elements[0]]
+                for i in range(1, len(group_elements)):
+                    prev_e = group_elements[i - 1]
+                    curr_e = group_elements[i]
+                    v_gap = curr_e.bbox[1] - prev_e.bbox[3]
+                    
+                    # Hanging indent check: if current line starts significantly left of previous line
+                    is_outdent = (prev_e.bbox[0] - curr_e.bbox[0]) > (avg_font_size * 0.5)
+                    # Gap check
+                    is_gap = v_gap > vg_thresh
+                    
+                    if is_gap or is_outdent:
+                        subgroups.append(curr_sg)
+                        curr_sg = [curr_e]
+                    else:
+                        curr_sg.append(curr_e)
+                if curr_sg: subgroups.append(curr_sg)
+            else:
+                subgroups = [group_elements]
+
+            for sg_idx, sg in enumerate(subgroups):
+                if group_idx > 0 or sg_idx > 0:
+                    p = docx_doc.add_paragraph(style=style_name)
+                    p.paragraph_format.left_indent = Inches(indent_from_margin_in)
+                    p.paragraph_format.right_indent = Inches(right_indent_from_margin_in)
+                    p.paragraph_format.space_after = Pt(2)
+                    if block_type == "title":
+                        p.alignment = WD_ALIGN_PARAGRAPH.CENTER
+                    elif block_type in ["figure_caption", "table_caption"]:
+                        p.alignment = WD_ALIGN_PARAGRAPH.LEFT
+                    elif block_type not in ["formula_caption"] and not style_name.startswith("Heading"):
+                        p.alignment = WD_ALIGN_PARAGRAPH.JUSTIFY
+                
+                for elem in sg:
+                    text_content = elem.text
+                    if not text_content.endswith(" "):
+                        text_content += " "
+                    run = p.add_run(sanitize_text_for_xml(text_content))
+                    _apply_span_style(run, elem)
     else:
         font_sizes = [e.font_size for e in block.elements if e.font_size is not None]
         avg_font_size = sum(font_sizes) / len(font_sizes) if font_sizes else 12.0
diff --git a/backend/src/pipeline.py b/backend/src/pipeline.py
index 36cd402..ef20a93 100644
--- a/backend/src/pipeline.py
+++ b/backend/src/pipeline.py
@@ -16,7 +16,8 @@ from typing import Callable, Dict, List, Optional, Tuple
 import numpy as np
 import pymupdf
 from docx import Document
-from docx.enum.text import WD_ALIGN_PARAGRAPH
+from docx.enum.section import WD_SECTION
+from docx.enum.text import WD_ALIGN_PARAGRAPH, WD_BREAK
 from docx.oxml import OxmlElement
 from docx.oxml.ns import qn
 from docx.shared import Inches, Pt, RGBColor
@@ -52,6 +53,76 @@ PAGE_NUMBER_BLOCK_TYPES = {
     "pagination",
 }
 
+def _detect_2_columns(
+    layout_blocks: List[LayoutBlock], page_width: float
+) -> Optional[Tuple[List[LayoutBlock], List[LayoutBlock], List[LayoutBlock], List[LayoutBlock]]]:
+    """
+    Detects a 2-column layout using horizontal center points.
+
+    Classification:
+      - A block whose center is near the page center AND whose width >= 50%
+        of the page is a full-width **breaker** (title, author block, etc.).
+      - Otherwise the block belongs to the **left** or **right** column
+        depending on whether its center is left or right of the page center.
+
+    Returns (top_breakers, left_blocks, right_blocks, bottom_breakers)
+    if a 2-column layout is detected, or None for single-column.
+    """
+    if not layout_blocks or page_width <= 0:
+        return None
+
+    page_center = page_width / 2.0
+    gutter_width = 15.0  # pt
+
+    breakers: List[LayoutBlock] = []
+    left_blocks: List[LayoutBlock] = []
+    right_blocks: List[LayoutBlock] = []
+
+    for b in layout_blocks:
+        if b.block_type == "abandon":
+            continue
+
+        cx = (b.bbox[0] + b.bbox[2]) / 2.0
+        block_width = b.bbox[2] - b.bbox[0]
+
+        # Crosses the gutter OR wide enough → full-width breaker
+        crosses_gutter = (b.bbox[0] < page_center - gutter_width) and (b.bbox[2] > page_center + gutter_width)
+        if crosses_gutter or block_width >= page_width * 0.50:
+            breakers.append(b)
+        elif cx < page_center:
+            left_blocks.append(b)
+        else:
+            right_blocks.append(b)
+
+    if not left_blocks or not right_blocks:
+        return None
+
+    # Validate columns sit side-by-side (vertical overlap >= 10pt)
+    left_y0 = min(b.bbox[1] for b in left_blocks)
+    left_y1 = max(b.bbox[3] for b in left_blocks)
+    right_y0 = min(b.bbox[1] for b in right_blocks)
+    right_y1 = max(b.bbox[3] for b in right_blocks)
+
+    overlap = min(left_y1, right_y1) - max(left_y0, right_y0)
+    if overlap < 10.0:
+        return None
+
+    # Sort blocks top-to-bottom within their columns
+    left_blocks.sort(key=lambda b: (b.bbox[1], b.bbox[0]))
+    right_blocks.sort(key=lambda b: (b.bbox[1], b.bbox[0]))
+
+    # Separate breakers into top / bottom based on the column start.
+    # A breaker whose bottom edge is at or above the first column block
+    # is a top breaker; everything else is a bottom breaker.
+    col_top = min(left_y0, right_y0)
+    top_breakers = [b for b in breakers if b.bbox[3] <= col_top + 5.0]
+    bottom_breakers = [b for b in breakers if b not in top_breakers]
+
+    top_breakers.sort(key=lambda b: (b.bbox[1], b.bbox[0]))
+    bottom_breakers.sort(key=lambda b: (b.bbox[1], b.bbox[0]))
+
+    return top_breakers, left_blocks, right_blocks, bottom_breakers
+
 
 def _block_text(block: LayoutBlock) -> str:
     text = getattr(block, "text", "") or ""
@@ -153,6 +224,47 @@ def _is_original_page_number_block(
     return centered and near_bottom and footer_sized
 
 
+def _filter_margin_blocks(
+    layout_blocks: List[LayoutBlock], page_width: float, page_height: float
+) -> List[LayoutBlock]:
+    """
+    Filter out blocks sitting in page margins (e.g., rotated arXiv sidebar
+    annotations like ``arXiv:2606.14730v1 [cs.CV] 2 Jun 2026``).
+
+    Applied *before* column detection so that margin text does not pollute
+    the left/right column boundary calculations.
+    """
+    left_strict = page_width * 0.05
+    right_strict = page_width * 0.95
+    left_relaxed = page_width * 0.10
+    right_relaxed = page_width * 0.90
+    narrow_threshold = 15.0  # pt
+
+    filtered: List[LayoutBlock] = []
+    for b in layout_blocks:
+        # Always keep figure and table blocks
+        if b.block_type in ("figure", "table"):
+            filtered.append(b)
+            continue
+
+        x0, y0, x1, y1 = b.bbox
+        block_width = x1 - x0
+
+        # Entire block inside strict left-margin zone
+        if x1 < left_strict:
+            continue
+        # Entire block inside strict right-margin zone
+        if x0 > right_strict:
+            continue
+        # Very narrow block inside relaxed margin zone
+        if block_width < narrow_threshold and (x1 < left_relaxed or x0 > right_relaxed):
+            continue
+
+        filtered.append(b)
+
+    return filtered
+
+
 def _horizontal_overlap_width(bbox_a, bbox_b) -> float:
     return max(0.0, min(bbox_a[2], bbox_b[2]) - max(bbox_a[0], bbox_b[0]))
 
@@ -311,6 +423,21 @@ class PDFToDocxPipeline:
 
         logger.info(f"[Timing] YOLO inference (all batches) took: {time.time() - t_yolo_start:.4f}s")
 
+        # Global Multi-Column Detection Pre-scan
+        self.is_multicolumn_pdf = False
+        class DummyBlock:
+            def __init__(self, bbox):
+                self.bbox = bbox
+                self.block_type = "text"
+        
+        for i, det_dicts in enumerate(all_det_dicts):
+            p_width = pdf_doc[start_page + i].rect.width
+            dummy_blocks = [DummyBlock(d["bbox"]) for d in det_dicts if d["class_name"] not in ["abandon", "table", "figure"]]
+            if _detect_2_columns(dummy_blocks, p_width) is not None:
+                self.is_multicolumn_pdf = True
+                break
+        logger.info(f"Global Document Layout: {'Multi-column' if self.is_multicolumn_pdf else 'Single-column'}")
+
         if progress_callback:
             progress_callback(45)
 
@@ -362,6 +489,7 @@ class PDFToDocxPipeline:
                 pdf_elements=pdf_elements,
                 det_dicts=det_dicts,
                 page_idx=page_idx,
+                start_page=start_page,
                 page_folder="",
                 docx_doc=docx_doc,
                 doc_left_margin_in=doc_left_margin_in,
@@ -430,6 +558,7 @@ class PDFToDocxPipeline:
         pdf_elements: List[Dict],
         det_dicts: List[Dict],
         page_idx: int,
+        start_page: int,
         page_folder: str,
         docx_doc: Document,
         doc_left_margin_in: float,
@@ -582,7 +711,8 @@ class PDFToDocxPipeline:
             if not _is_original_page_number_block(block, page.rect.width, page_height)
         ]
 
-        layout_blocks = sorted(layout_blocks, key=lambda b: (b.bbox[1], b.bbox[0]))
+        # Filter margin annotations (e.g., arXiv sidebar) before column detection
+        layout_blocks = _filter_margin_blocks(layout_blocks, page.rect.width, page_height)
 
         content_blocks_for_y1 = [b for b in layout_blocks if b.block_type != "abandon"]
         this_page_last_content_y1 = max((b.bbox[3] for b in content_blocks_for_y1), default=0.0)
@@ -592,27 +722,96 @@ class PDFToDocxPipeline:
                 block.extra = {}
             block.extra['page_idx'] = page_idx
             block.extra['page_height'] = page_height
+            block.extra['is_multicolumn_pdf'] = getattr(self, "is_multicolumn_pdf", False)
 
-        # Group blocks into rows
-        rows = []
-        current_row = []
-        row_threshold = 10
-        for b in layout_blocks:
-            top_y = b.bbox[1]
-            if not current_row:
-                current_row = [b]
-                current_top = top_y
+        if self.is_multicolumn_pdf:
+            columns_data = _detect_2_columns(layout_blocks, page.rect.width)
+        else:
+            columns_data = None
+
+        class DummyBreakBlock:
+            def __init__(self, break_type, y):
+                self.block_type = break_type
+                self.bbox = (0, y, 0, y)
+                self.text = ""
+                self.score = 1.0
+                self.elements = []
+                self.extra = {}
+
+        if columns_data is not None:
+            top_breakers, left_blocks, right_blocks, bottom_breakers = columns_data
+            print("  Detected 2-column layout.")
+
+            # Calculate col_x0 to preserve relative indents
+            left_col_x0 = min((b.bbox[0] for b in left_blocks), default=0)
+            right_col_x0 = min((b.bbox[0] for b in right_blocks), default=0)
+
+            # Tag column blocks so process_text_block computes relative indent
+            for b in left_blocks:
+                b.extra["in_column"] = True
+                b.extra["col_left_x0_pt"] = left_col_x0
+                b.extra["is_multicolumn_pdf"] = True
+            for b in right_blocks:
+                b.extra["in_column"] = True
+                b.extra["col_left_x0_pt"] = right_col_x0
+                b.extra["is_multicolumn_pdf"] = True
+
+            rows = []
+            col_start_y1 = None
+            
+            # 1-col section for top breakers (prevents inheriting 2-col from previous page)
+            if top_breakers:
+                b = DummyBreakBlock("section_1col", 0)
+                if page_idx > start_page: b.extra["is_new_page"] = True
+                rows.append([b])
+                
+                for b_blk in top_breakers: rows.append([b_blk])
+                col_start_y1 = max((b_blk.bbox[3] for b_blk in top_breakers), default=0)
             else:
-                if abs(top_y - current_top) <= row_threshold:
-                    current_row.append(b)
-                else:
-                    rows.append(sorted(current_row, key=lambda x: x.bbox[0]))
+                col_start_y1 = prev_page_last_content_y1 if prev_page_last_content_y1 else 0
+
+            # 2-col section for column content
+            b2 = DummyBreakBlock("section_2col", 0)
+            if not top_breakers and page_idx > start_page:
+                b2.extra["is_new_page"] = True
+            rows.append([b2])
+            
+            for b_blk in left_blocks: rows.append([b_blk])
+            
+            # column_break with reset_y1 so the right column aligns with the left
+            cb_block = DummyBreakBlock("column_break", 0)
+            cb_block.extra["reset_y1"] = col_start_y1
+            rows.append([cb_block])
+            
+            for b in right_blocks: rows.append([b])
+            
+            # 1-col section for bottom breakers
+            if bottom_breakers:
+                rows.append([DummyBreakBlock("section_1col", 0)])
+                for b in bottom_breakers: rows.append([b])
+        else:
+            layout_blocks = sorted(layout_blocks, key=lambda b: (b.bbox[1], b.bbox[0]))
+            
+            # Group blocks into rows
+            rows = []
+            current_row = []
+            row_threshold = 10
+            for b in layout_blocks:
+                top_y = b.bbox[1]
+                if not current_row:
                     current_row = [b]
                     current_top = top_y
-        if current_row:
-            rows.append(sorted(current_row, key=lambda x: x.bbox[0]))
+                else:
+                    if abs(top_y - current_top) <= row_threshold:
+                        current_row.append(b)
+                    else:
+                        rows.append(sorted(current_row, key=lambda x: x.bbox[0]))
+                        current_row = [b]
+                        current_top = top_y
+            if current_row:
+                rows.append(sorted(current_row, key=lambda x: x.bbox[0]))
 
-        print(f"  Grouped into {len(rows)} rows")
+        print(f"  Grouped into {len(rows)} rows/items")
 
         # Detect intentional page breaks
         first_content_block = next((b for b in layout_blocks if b.block_type != "abandon"), None)
@@ -662,6 +861,51 @@ class PDFToDocxPipeline:
             for block in row:
                 block_type = block.block_type
 
+                if block_type == "section_2col":
+                    sect_type = WD_SECTION.NEW_PAGE if block.extra.get("is_new_page") else WD_SECTION.CONTINUOUS
+                    new_section = docx_doc.add_section(sect_type)
+                    sectPr = new_section._sectPr
+                    cols = OxmlElement('w:cols')
+                    cols.set(qn('w:num'), '2')
+                    cols.set(qn('w:space'), '720') # 0.5 inch gap
+                    sectPr.append(cols)
+                    section = new_section
+                    last_text_paragraph = None
+                    prev_text_block = None
+                    continue
+
+                if block_type == "section_1col":
+                    sect_type = WD_SECTION.NEW_PAGE if block.extra.get("is_new_page") else WD_SECTION.CONTINUOUS
+                    new_section = docx_doc.add_section(sect_type)
+                    sectPr = new_section._sectPr
+                    cols = OxmlElement('w:cols')
+                    cols.set(qn('w:num'), '1')
+                    sectPr.append(cols)
+                    section = new_section
+                    last_text_paragraph = None
+                    prev_text_block = None
+                    continue
+
+                if block_type == "column_break":
+                    if len(docx_doc.paragraphs) > 0:
+                        p = docx_doc.paragraphs[-1]
+                        if not p.text.strip():
+                            run = p.add_run()
+                        else:
+                            p = docx_doc.add_paragraph()
+                            run = p.add_run()
+                    else:
+                        p = docx_doc.add_paragraph()
+                        run = p.add_run()
+                    run.add_break(WD_BREAK.COLUMN)
+                    
+                    if "reset_y1" in block.extra:
+                        prev_row_y1 = block.extra["reset_y1"]
+                        
+                    last_text_paragraph = None
+                    prev_text_block = None
+                    continue
+
                 if block_type == "abandon":
                     continue
 
